<Page
    x:Class="WxaCode.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WxaCode"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <TextBlock x:Name="AppVersionTextBlock" Margin="0,0,0,10" />
            <ToggleSwitch Header="退出应用时在应用本地保存 appid 和 appsecret" x:Name="SaveAppidAndAppsecretSwitch" Toggled="SaveAppidAndAppsecret_Toggled" />
            <ToggleSwitch Header="退出应用时在应用本地保存 access_token" x:Name="SvaveAppAccessTokenSwitch" Toggled="SvaveAppAccessToken_Toggled" Margin="0,10,0,0"/>
            <ToggleSwitch Header="退出应用时在应用本地保存小程序码参数" x:Name="SaveWxacodeParamSwitch" Toggled="SaveWxacodeParam_Toggled" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
